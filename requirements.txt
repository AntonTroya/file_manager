# Не требуется никаких внешних зависимостей - используется только стандартная библиотека Python
# tkinter входит в состав Python для Windows и macOS
# В Linux вам может потребоваться установить: sudo apt-get install python3-tk